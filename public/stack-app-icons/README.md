Add app icon assets here as WebP files when possible (preferred for performance).

Expected filenames should match whatever is used in `data/stack.js` (including extension).

Rendering order:
1) appIcon
2) logo
3) logoFallback
4) first letter fallback
